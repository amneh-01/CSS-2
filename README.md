# CSS-2  9/9

1 Selectors: Pseudo-elements (::before, ::after).

2- Positioning:
static, relative, absolute, fixed, sticky.

3- Display Property:
block, inline, inline-block, none.

4- CSS Flexbox:
Container properties (flex-direction,flex-wrap,align-content,justify-content,align-items , gap).

5- CSS Grid:
Basic syntax: grid-template-rows, grid-template-columns.
Container properties(justify-content,align-items,justify-items,Container properties,gap , grid-column, grid-row)

6- Floating
